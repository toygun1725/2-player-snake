<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- v3.3.1: Bildirim izni (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- GPGPC: Touchscreen is not required (PCs don't have touchscreens) -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".SnakeApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:resizeableActivity="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TwoPlayerSnake">

        <!-- Google Play Games Services App ID -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="423239030301" />

        <!-- v3.3.2 - Prevent automatic/forced sign-in popup at game launch -->
        <meta-data
            android:name="com.google.android.gms.games.PLAY_GAMES_PROFILE_CREATION"
            android:value="optional" />

        <!-- AdMob Application ID (Production) -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4114535776207741~8941104521" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.TwoPlayerSnake" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard|navigation|screenLayout|uiMode"
            android:theme="@style/Theme.TwoPlayerSnake.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="2playersnake.com" android:pathPrefix="/invite" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".ui.SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.TwoPlayerSnake.Splash">
        </activity>

    </application>

</manifest>
